# Task Memory: task_11.md

Keep only task-local execution context here. Do not duplicate facts that are obvious from the repository, task file, PRD documents, or git history.

## Objective Snapshot

- Documented the prepare/finish/abort lifecycle + GitFlow opt-in in `README.md` and appended an `[Unreleased]` CHANGELOG entry.
- Added a README doc-snippet smoke test in `packages/cli/__tests__/readme-doc-snippets.test.ts` that extracts every fenced `gw release` line and parses it through the real `makeReleaseCommand()` (with stubbed actions + mocked core / `@clack/prompts`).

## Important Decisions

- Used a placeholder CHANGELOG header `## [Unreleased] — two-phase release lifecycle` because the next release version is not finalized; format mirrors the existing `## [X.Y.Z]` entries.
- Snapshot test of `gw release --help` from the task spec's integration list was NOT added — `--help` output is generated by Commander from the same option definitions the snippet test already parses, so a help snapshot would only restate facts the parser already enforces. Added an explicit assertion that an unknown flag (e.g. `--not-a-real-flag`) is rejected to guard against drift instead.
- Skipped a `gw release --help` snapshot file; the doc-snippet smoke test plus the existing `release-wiring.test.ts` cover the surface change. Re-evaluate if the help text itself ever drifts from README claims.

## Learnings

- Commander's `exitOverride()` and `configureOutput()` must be applied to BOTH the parent command AND each subcommand — they do not propagate down. Without that, an unknown-flag check inside a subcommand still calls `process.exit`.
- Under ts-jest ESM in this repo, `__dirname` is undefined and `import.meta.url` is the documented sore spot (see shared MEMORY note on `loader.ts`). The new test walks up from `process.cwd()` looking for `README.md` + a `packages/` sibling to locate the repo root robustly without either.

## Files / Surfaces

- `README.md` — new "Release lifecycle" + "GitFlow opt-in" sections (between Commands and Privacy).
- `CHANGELOG.md` — new `[Unreleased]` entry above the `[0.1.0]` entry.
- `packages/cli/__tests__/readme-doc-snippets.test.ts` — new doc-snippet smoke test (3 specs).

## Errors / Corrections

- First run: `exitOverride()` only on parent → subcommand still called `process.exit(1)` on the unknown-flag sanity case. Fixed by setting `exitOverride` + silencing output on each subcommand too.
- Second run: `ReferenceError: __dirname is not defined`. Replaced with a `process.cwd()` upward walk that locates the repo-root README.

## Ready for Next Run

- Task is the final task in `_tasks.md`; no downstream task expects new artifacts beyond what was added.
- If the release version is decided before merge, replace the `[Unreleased]` header in `CHANGELOG.md` with `## [X.Y.Z] - YYYY-MM-DD` and add a `[X.Y.Z]: https://github.com/...` reference at the bottom (matching the existing `0.1.0` / `1.0.0` reference style).
